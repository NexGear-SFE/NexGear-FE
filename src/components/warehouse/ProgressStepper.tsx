import { Check } from 'lucide-react'
import { cn } from '@/utils/cn'
type Props = { steps: string[]; currentStep: number; errorStep?: number | null }
export function ProgressStepper({ steps, currentStep, errorStep = null }: Props) {
  return <ol aria-label="Tiến độ" className="grid gap-2 sm:grid-flow-col sm:auto-cols-fr">{steps.map((step, index) => { const complete = index < currentStep; const current = index === currentStep; const hasError = index === errorStep; return <li key={step} aria-current={current ? 'step' : undefined} aria-label={hasError ? `${step} có lỗi` : undefined} className={cn('flex min-h-12 items-center gap-3 rounded-sm border px-3 text-xs font-semibold', current && !hasError && 'border-brand-500 bg-brand-50 text-brand-500', complete && !hasError && 'border-success-200 bg-success-50 text-success-700', !current && !complete && !hasError && 'border-surface-400 bg-white text-text-600', hasError && 'border-error-700 bg-error-50 text-error-700')}><span className={cn('flex h-6 w-6 shrink-0 items-center justify-center rounded-full text-[11px]', complete && !hasError ? 'bg-success-500 text-white' : current && !hasError ? 'bg-brand-500 text-white' : hasError ? 'bg-error-700 text-white' : 'bg-surface-200')}>{complete && !hasError ? <Check className="h-3.5 w-3.5" /> : index + 1}</span><span>{step}</span></li>})}</ol>
}
