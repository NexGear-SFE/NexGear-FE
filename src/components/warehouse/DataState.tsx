import { AlertTriangle, Inbox, RefreshCw } from 'lucide-react'
type Props = { type: 'empty' | 'error'; title: string; description: string; onRetry?: () => void }
export function DataState({ type, title, description, onRetry }: Props) { const Icon = type === 'error' ? AlertTriangle : Inbox; return <div className="flex min-h-64 flex-col items-center justify-center rounded-md border border-dashed border-surface-400 bg-white p-8 text-center"><span className="mb-4 flex h-12 w-12 items-center justify-center rounded-md bg-surface-200 text-text-600"><Icon className="h-6 w-6" /></span><h2 className="font-heading text-base font-semibold">{title}</h2><p className="mt-2 max-w-md text-body-sm text-text-600">{description}</p>{onRetry && <button type="button" className="btn-outlined mt-5" onClick={onRetry}><RefreshCw className="h-4 w-4" /> Thử lại</button>}</div> }
